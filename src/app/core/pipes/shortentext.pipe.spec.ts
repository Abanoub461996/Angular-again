import { ShortentextPipe } from './shortentext.pipe';

describe('ShortentextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortentextPipe();
    expect(pipe).toBeTruthy();
  });
});
